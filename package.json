{
  "name": "sutil-helloworld",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "dotnet fable src/App -o fable-build && vite fable-build",
    "start": "dotnet fable watch src/App --verbose -o fable-build --run vite",
    "preview": "vite preview",
    "clean": "dotnet fable clean -o fable-build --yes"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
